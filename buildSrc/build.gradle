/**
 * This build.gradle is used to perform custom task functions when the package
 * is called from the build.gradle one directory above. This is used to call
 * and download dependencies of the custom Gradle Task.
 */